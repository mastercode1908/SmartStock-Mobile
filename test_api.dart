import 'dart:convert';
import 'package:http/http.dart' as http;

void main() async {
  try {
    final baseUrl = 'https://smartstock1908-001-site1.ltmp.net/api'; // guessing URL from previous sessions or typical smart stock url
    // Let's first search where the URL is defined
  } catch (e) {
    print(e);
  }
}
